# Description: A tool which allows you to compose wallpapers for X.
# URL:         http://www.thegraveyard.org/hsetroot.php
# Maintainer:  doom, dm <at> archlinux <dot> info
# Depends on:  xorg-server imlib2

name=hsetroot
version=1.0.2
release=1
source=("http://www.thegraveyard.org/files/$name-$version.tar.gz")

build() {
  cd "$SRC/$name-$version"

  ./configure --prefix=/usr
  make

  cd "$SRC/$name-$version"
  make DESTDIR="$PKG" install
}
