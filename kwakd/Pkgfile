# Description: tiny web server serving blank html pages
# URL:         https://code.google.com/p/kwakd/
# Maintainer:  Maximilian Dietrich, dm <at> archlinux <dot> info
# Packager:    Maximilian Dietrich, dm <at> archlinux <dot> info
# Depends on: 

name=kwakd
version=0.3
release=1
source=(https://kwakd.googlecode.com/files/kwakd-0.3.tgz kwakd)

build () {
    cd "$name-$version"
    ./configure --prefix=/usr --mandir=/usr/man
    make
    make DESTDIR=$PKG \
        install
    
    install -D -m 755 $SRC/kwakd $PKG/etc/rc.d/kwakd
}
