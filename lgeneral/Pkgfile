# Description: turn-based strategy engine and game similar to Panzer General
# URL:         http://lgames.sourceforge.net/index.php?project=LGeneral
# Maintainer:  Maximilian Dietrich, dm <at> archlinux <dot> info
# Depends on:  libsdl sdl_mixer

name=lgeneral
version=1.2.3
release=1
source=(http://downloads.sourceforge.net/${name}/${name}-${version}.tar.gz http://downloads.sourceforge.net/$name/pg-data.tar.gz)

build () {
    cd "$name-$version"
    ./configure --prefix=/usr --mandir=/usr/man
    make
    make DESTDIR=$PKG \
        install

    $PKG/usr/bin/lgc-pg -s $SRC/pg-data -d $PKG/usr/share/lgeneral
    rm -rf $PKG/usr/share/locale
}
