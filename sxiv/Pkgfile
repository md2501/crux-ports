# Description: simple/suckless image-viewer
# URL:         https://github.com/muennich/sxiv.html 
# Maintainer:  Maximilian Dietrich, dxm <at> openmailbox <dot> org
# Depends on:  imlib2

name=sxiv
version=1.1.1
release=1
source=(https://github.com/muennich/$name/archive/v$version.zip config.h)

build () {
    cd "$name-$version"
    cp $SRC/config.h .
    make
    make DESTDIR=$PKG \
        PREFIX=/usr \
        MANPREFIX=/usr/man \
        install
}
