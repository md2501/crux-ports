# Description:	Bind keys to shell scripts in X
# URL:		http://www.nongnu.org/xbindkeys/xbindkeys.html
# Maintainer:	Maximilian Dietrich, dm <at> archlinux <dot> info	
# Packager:	Maximilian Dietrich, dm <at> archlinux <dot> info
# Depends on:	xorg

name=xbindkeys
version=1.8.5
release=1
source=(http://www.nongnu.org/$name/$name-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr --mandir=/usr/man --disable-tk --disable-guile
	make
	make DESTDIR=$PKG install
}
